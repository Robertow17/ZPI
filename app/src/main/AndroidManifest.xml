<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zpi">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".checklist.activities.CheckListActivity"></activity>
        <activity android:name=".TEST"></activity>
        <activity android:name=".favourites.FavouritesActivity" />
        <activity android:name=".serviceProvider.activities.AddService" />
        <activity android:name=".serviceProvider.activities.EditService" />
        <activity android:name=".serviceProvider.activities.ServiceProviderMainActivity" />
        <activity android:name=".guests.activities.GuestsListActivity" />
        <activity android:name=".searcher.activities.ServiceDetails" />
        <activity
            android:name=".searcher.activities.SearcherActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".budget.activities.DeleteExpenseActivity" />
        <activity android:name=".budget.activities.AddExpenseActivity" />
        <activity android:name=".budget.activities.BudgetActivity" />
        <activity android:name=".budget.activities.EditExpenseActivity" />
        <activity android:name=".calendar.activities.CalendarActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".loginForm.activities.LoginForm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>